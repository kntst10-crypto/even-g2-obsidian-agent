# Vault Lens — 提出前チェック

## 最新: 固定HTTPS中継方式を選択し0.2を実装

2026-09-13に一般公開向けの固定HTTPS中継方式が承認され、G2 UI統合・音声送信確認/文字起こし・端末ペアリング・PC outboundコネクター・manifest/buildを追加しました。詳細は`PUBLIC-EDITION.md`。接続方式は決定済みなので、下記旧チェックの「方式選択待ち」は解消しています。

**依然として未提出**です。実際のHTTPS中継配置、実ドメインを使う再ビルド、WebView CORS originの確定、AIキー/モデル/予算、連絡先と最終プライバシーURL、実機Beta、実Simulator画像が必要です。`vault-lens-dev.ehpk`は`.invalid` originの開発成果物であり提出しないでください。

以下は0.1時点の履歴です。

確認日: 2026-09-13

## 結論

**未提出。現在のビルドはEven Hub審査に進められる完成状態ではありません。**
ブラウザMVPに加えてG2アダプターを準備しましたが、UI統合、音声文字起こし、リモート接続、パッケージ作成、実機ベータテストは未完了です。

## 新たに確認された設計上のブロッカー

公式Networking文書では、通信先は`app.json`のnetwork whitelistへ**完全なoriginを個別列挙**する必要があり、裸のホスト名・ワイルドカードは非対応です。
したがって、一つの公開パッケージから各利用者が任意に指定するPC／Tailscaleドメインへ接続する構成は、そのままでは実装できません。

公開までに以下のどちらかを決定する必要があります。

1. 利用者別の接続先を固定した個人テストビルド。まず所有者のG2で検証する用途。一般公開版ではない。
2. 開発者が管理する固定HTTPS中継サービス。一般公開向けだが、新しいサービス運用、認証・ペアリング設計、費用、ドメイン、プライバシー判断が必要。

ユーザーの実Vaultを第三者サーバーに預ける、任意の有料サービスを契約する、接続先を勝手に公開する操作は行っていません。

## この確認で追加したもの

- `@evenrealities/even_hub_sdk` 0.0.15、CLI 0.1.14、Simulator 0.9.5を固定導入。
- Vite 8.3.0、TypeScript 5.9.3を開発依存として導入。ビルド設定は未作成。
- `src/g2-bridge.mjs`: 起動画面、テキストページ、ジェスチャー振分け、PCM録音上限30秒、停止・破棄、システム確認付き終了のアダプター。
- アダプターは**単体の準備実装**。現在の`public/app.js`には接続していません。
- 4件のアダプター単体テストを追加。既存14件と合計18件成功。

## 提出ゲート

| 項目 | 状態 |
| --- | --- |
| 既存Vault／AIアダプター／HTTPテスト | 14件成功（AI外部通信はモック） |
| G2アダプター単体テスト | 4件成功（bridgeはモック） |
| G2 UI統合 | 未完了 |
| 音声文字起こしと確認操作 | 未実装 |
| 固定HTTPS接続先とCORS | 未決定 |
| 初回設定の保存・復元 | 未実装 |
| app.jsonと実際の通信先whitelist | 未作成 |
| .ehpk作成・package_id空き確認 | 未実施 |
| モノクロ前景・背景アイコン | 未作成 |
| 実際のSimulatorスクリーンショット | 未取得 |
| 最終プライバシーポリシー・運用連絡先 | 接続設計決定後に確定 |
| 実機Betaで5分ロック・操作完遂・終了・再起動 | 未実施 |
| 審査提出 | 未実施 |

## 公式QAで必要とされる確認

- ルート画面のダブルタップは`bridge.shutDownPageContainer(1)`によるシステム終了確認。即時終了mode 0は不可。
- 設定が必要な初回起動はG2へ説明を出し、設定を保存して次回に復元する。
- スマートフォンをロックしたままコア操作をG2／R1だけで完遂する。
- 実際のBeta Testingビルドでロック5分・終了・Conversate再起動を確認する。SimulatorやQRローカル実行のみでは代替できない。
- ストア画像はモノクロ、前景・背景を両方用意。スクリーンショットはSimulatorの実表示から取得する。
- アプリ名・ストア名・G2上の名称を一致させる。Vault Lensは20文字以内、Evenの名前を含まない。
- 医療診断、金融助言、緊急誘導を製品機能として掲げない。ノート検索・下書き・確認付き追記に限定する。

## 公式資料

- https://hub.evenrealities.com/docs/build/networking — origin列挙、ワイルドカード非対応、HTTPS、CORS。
- https://hub.evenrealities.com/docs/ship/packaging — manifest、秘密鍵同梱禁止、permissions、CLIパック。
- https://hub.evenrealities.com/docs/ship/app-submission — QA全項目、Betaの5分ロック検証、提出状態遷移。
- https://hub.evenrealities.com/docs/build/device-apis — PCM 16kHz・16bit LE mono、音声権限、入力。
- インストール済みSDKの`dist/index.d.ts` — 0.0.15のメソッドと返値型を確認。

これは審査通過の保証ではなく、未解決事項を明確にするチェック記録です。
