RELAY_ORIGIN=https://relay.example.invalid
PORT=8790
# Exact Even Hub WebView origin(s), comma-separated; obtain from actual beta runtime.
# Never set '*' or reflect untrusted Origin values.
CLIENT_ORIGINS=
# Keep onboarding invite-only until abuse controls and budgets are configured.
PUBLIC_PAIRING=false
ONBOARDING_TOKEN=REPLACE_WITH_RANDOM_32_BYTE_SECRET
ALLOW_CLOUD=false
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_TRANSCRIBE_MODEL=
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=
