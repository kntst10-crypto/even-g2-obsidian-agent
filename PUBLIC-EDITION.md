# Vault Lens 0.2 — 固定HTTPS中継版

作成: 2026-09-13。**コード・開発パッケージまで実装。公開配置・実機Beta・審査提出は未完了。**

## 構成と機能

G2の配布物は一つの固定HTTPS originだけへ接続します。Windows PCのコネクターはポートを公開せず、同じ中継へ1秒ごとにoutbound HTTPSで仕事を取りに行きます。Vault本体はPCに残ります。

- `client/`: Even Hub SDKを統合したG2画面とスマートフォン設定画面。
- `src/relay.mjs`: 独立したNode.js 22の単一プロセス中継。Docker/Render等でTLS終端の背後に配置可能。
- `src/connector.mjs`: PC常駐処理。検索、Inbox/Daily/指定ノートへの追記提案・確認・取消だけを実行。
- `src/transcribe.mjs`: G2の16kHz/16bit mono PCMをWAVにし、OpenAIへ送信する文字起こし。最大30秒。録音後に送信確認。
- `src/providers.mjs`: ローカル検索抜粋、OpenAI、Claude。APIキーとモデルは中継側の環境変数。
- `public/` と `src/server.mjs`: 従来のローカルブラウザMVP。公開クライアントでは使用しません。

G2から上下で機能を選択し、タップで録音開始/停止、音声送信確認、文字起こし確認、追記提案の全ページを見た後に別タップで保存します。ダブルタップは取消/戻る、ホームではOSの終了確認。スマホからも文字入力で検索や追記を行えます。

検索はファイル名と本文のキーワード部分一致です。自然文の意味検索ではありません。AIへの質問は検索語を含めて短く話してください。LINE下書き/指定ノート追記はAPIと従来UIで利用できますが、G2のホームメニューは検索/Inbox/Daily/回答の4機能です。

## デプロイ手順

1. Node.js 22対応の単一インスタンスWebサービスを用意。`Dockerfile`と`render.yaml`は配置用の雛形です。サービス契約・課金をこの実装で自動承諾するものではありません。
2. `relay.env.example`を参考に中継側の環境変数を設定。`RELAY_ORIGIN`は実際の固定HTTPS origin、`CLIENT_ORIGINS`は実BetaのWebViewから送られる正確なorigin。ワイルドカード不可。Originが送信されないネイティブ通信は端末Bearer認証で保護します。
3. 初期状態は`PUBLIC_PAIRING=false`の招待Beta。`ONBOARDING_TOKEN`は運営者が発行したランダム値（32バイト以上）を設定。公開時に`PUBLIC_PAIRING=true`へ変更する前に、上流WAF/レート制限・サービス利用上限・AI予算・問い合わせ体制を準備してください。
4. 音声/AIが必要なら`ALLOW_CLOUD=true`、APIキー、利用可能な`OPENAI_MODEL` / `OPENAI_TRANSCRIBE_MODEL` / `ANTHROPIC_MODEL`を設定。モデルの有効性と請求先を実APIで確認。初期状態は外部AI OFFなので音声機能はまだ動きません。
5. `/health` がHTTPSで200を返すこと、本文やAuthorizationをアクセスログ/エラー監視/APMへ収集しないこと、TLS終端からアプリまでの運用保護を確認。
6. 実ドメインを設定した同一シェルで`RELEASE=true RELAY_ORIGIN=https://実ドメイン npm run pack`。`app.json`のwhitelistとJSの接続先が同時生成されます。開発プレースホルダー`.invalid`ではreleaseビルドを拒否します。出力名は`vault-lens-dev.ehpk`なので、実機検証後にrelease用名へコピーして識別してください。
7. Even Hubでpackage IDの利用可否確認、Beta配布、実機5分ロック/終了/再起動/音声/確認保存/失敗時の確認を実施してから審査へ進む。

Cloudflare Workers用のコードではありません。Node標準HTTPと揮発メモリを使うため、Workersへ配置するにはDurable Objects等への別実装が必要です。ベンダー固有APIに依存しないDockerイメージを優先しています。

## Windowsコネクター

1. Node.js 22以降をインストール。
2. `connector.env.example`を`.env.connector`という名前でコピーして、`VAULT_ROOT`、公開済み`RELAY_ORIGIN`、招待Beta時の`ONBOARDING_TOKEN`を設定。
3. 最初は実Vaultではなく`demo-vault`またはバックアップコピーを指定。
4. 専用フォルダで`npm run connector`を実行。表示された18桁コードを5分以内にEvenアプリへ入力。コードは一回限り。
5. コマンド画面を開いておく。PCがスリープ/停止すると操作できません。手動終了はCtrl+C。

ファイアウォールの受信ポート開放やTailscaleは不要です。配布用Windowsインストーラー/サービス登録はまだありません。自動起動は実機確認後にWindowsタスクスケジューラでこのコマンドを登録します。

## 保存・認証・制約

| データ | 保持方法 |
| --- | --- |
| Vault本体 | PCの元フォルダ。全体アップロードなし |
| ジョブ本文/検索抜粋 | 中継メモリのみ。ジョブは最大45秒、応答で即削除。AI処理中の抜粋は追加で最大30秒 |
| 音声 | 最大30秒分を端末メモリに保持し送信確認。中継でファイル化せずAPIへ送り、API処理最大30秒 |
| 未確認の追記 | PCコネクターのメモリで5分。期限後5秒以内の定期掃除、再起動でも消去 |
| 接続コード | 暗号乱数72bit、一回限り5分。中継にはハッシュのみ |
| 端末/PCトークン | 256bitの別トークン。中継にはハッシュのみ。24時間・明示解除・中継再起動で失効 |
| 端末設定 | EvenアプリSDKのローカルストレージ。ブラウザプレビューではsessionStorage。OSの秘密ストレージ保証はしない |
| 監査ログ | 操作種別・HTTP結果・時刻のみ。本文、ノートパス、IP、コード、トークンは記録しない |

JavaScriptのメモリ開放はGCによるため物理メモリの即時ゼロ化を保証するものではありません。中継が平文を処理するTLS構成であり、エンドツーエンド暗号化ではありません。AI事業者の保持方針は別途適用され、ゼロ保持と断定しません。

上限: 単一プロセス1000セッション、同時4ジョブ/セッション、JSON 24KB、音声960044バイト、AI同時4件、ペアリング全体30回/分、認証トークンごと180回/分、全体6000回/分。悪用対策の完成版ではないため一般公開前に上流防御と利用者/予算管理が必要です。

中継再起動/24時間失効時は再ペアリングが必要で、継続接続用の永続的な認証DBはありません。複数インスタンスやオートスケールは不可です。スリープする無料ホストは接続維持に不向きです。

保存ジョブは配信後に自動再配信しません。応答が失われたときは『保存済み』とも『未保存』とも断定せず、PCのノートで確認してから再提案してください。同一PC上で悪意ある他プロセスがVaultフォルダを差し替える攻撃や厳密なファイルシステム取引保証は対象外です。

## リリース前に必要な実値

- 固定HTTPS origin、ホスティングの所有アカウント・費用上限。
- Even Hub実Betaの正確なWebView origin。
- OpenAI/Claude APIの管理キー、モデルID、請求/予算制限。
- 公開連絡先メール、プライバシーポリシー公開URL、ホスティング提供者と地域。
- package ID空き、実機Beta結果、実Simulatorスクリーンショット、アイコンの最終フォーマット。

`store/`の文面・SVGはドラフトです。スクリーンショットを捏造せず、実画面から取得して差し替えてください。
