# 検証結果

実施日: 2026-09-13 / 実行環境: Node.js v24.19.0 / Linux

## 自動検証

`npm test`: **18件成功、0件失敗**。

1. 日本語検索・出典・隠しファイル除外。
2. 提案時は未変更、確認時のみ追記、再確認拒否。
3. Daily Noteの新規作成は確認後のみ。
4. 取消・期限切れの提案は実行不可。
5. 外部編集で提案を無効化し、編集内容を保持。
6. 同時確認でも重複追記しない。
7. 相対パス逸脱、絶対パス、隠しパス、非Markdownなどを拒否。
8. シンボリックリンク・ハードリンクを拒否。
9. ローカルモードはネットワークを呼ばない。
10. クラウドはサーバー許可とリクエスト同意の両方が必要。
11. OpenAIの送信形式・鍵ヘッダー・store=false・本文抽出（モック）。
12. Claude Messagesの送信形式・本文抽出（モック）。
13. Dailyの日付が指定タイムゾーンに従う。
14. HTTP統合: 認証、Origin検証、検索、追記提案、明示確認。
15. G2起動画面: 表示内容・イベント受信・初期化重複防止（モック）。
16. G2ジェスチャー: 0値の互換性、音声イベントと空イベントを誤クリックしない（モック）。
17. G2音声: PCM結合・停止・バッファ破棄・確認モード1での終了（モック）。
18. G2起動失敗／未接続マイク操作のエラー（モック）。

`npm run check`: サーバー、Vault、APIアダプター、ブラウザJavaScriptの構文検査成功。

## 未検証・未実装

- G2実機とEvenアプリ、SDKの接続、マイク、タッチ、文字起こし。
- スマートフォンブラウザの表示と操作、画面の見た目の目視検証。
- OpenAI / Claudeの実API呼出し、モデル権限、回答品質、料金。
- リモートHTTPS/私設ネットワーク経由の接続と切断復旧。
- Windows / macOS、ObsidianやSyncとの並行編集。
- セキュリティ監査、負荷試験、バックアップ/復旧、監査ログ。
- Even Hub配布パッケージとストア審査。

自動テスト成功は、実機対応完了・一般公開可能という意味ではありません。
